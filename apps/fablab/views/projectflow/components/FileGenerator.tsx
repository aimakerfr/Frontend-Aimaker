import React from 'react';

const FileGenerator: React.FC = () => {
  // TODO: WHen mounting bring the maker_path_variables for this maker path using the prop makerPathId
  // TODO: Create a logic for ensamble the index, given each variable, get the source url to inject in html
  // TODO: use a button to trigger the file generation logic
  // TODO: After the file generation, download the file.
  return <div />;
};

export default FileGenerator;
